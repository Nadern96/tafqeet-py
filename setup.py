from setuptools import setup

setup(name='tafqeet',
      version='1.0',
      description='python pkg for converting numbers to arabic words',
      author='nader',
      install_requires=[],
      packages=['tafqeet'],
      zip_safe=False)
